# Environment Configuration Example
# Copy this file to .env and fill in your values

# PHP Environment
PHP_ENV=development

# SSL Configuration (for development, use self-signed certificates)
SSL_CERTIFICATE=/etc/nginx/ssl/cert.pem
SSL_CERTIFICATE_KEY=/etc/nginx/ssl/key.pem

# Database (if you add a database module later)
# DB_HOST=database
# DB_PORT=5432
# DB_NAME=transcendence
# DB_USER=transcendence_user
# DB_PASSWORD=your_secure_password_here

# JWT (if you add JWT module later)
# JWT_SECRET=your_jwt_secret_here
# JWT_EXPIRATION=3600

# OAuth (if you add OAuth module later)
# OAUTH_CLIENT_ID=your_client_id
# OAUTH_CLIENT_SECRET=your_client_secret
# OAUTH_CALLBACK_URL=https://localhost/api/auth/callback

# Application
APP_URL=https://localhost
APP_PORT=443
